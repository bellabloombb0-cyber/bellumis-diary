# Bellumi’s Diary

Eine Next.js-16-One-Page-Website mit Tailwind CSS 4.

## Starten

1. `npm install`
2. `npm run dev`

Die editierbaren Texte stehen direkt in den Komponenten. Der Instagram-Link ist bewusst als Platzhalter in `components/Contact.tsx` hinterlegt.
